import React from 'react';
import { CardRecettes } from '../Components/Card/Card';

const Recettes = () => {
    return (
        <div style={{marginTop:"165px"}}>
            <CardRecettes titre="Recettes" />
        </div>
    );
};

export default Recettes;